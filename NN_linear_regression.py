# -*- coding: utf-8 -*-
"""Копия_блокнота__практика_2_16_11_.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1neELNvahgEVdv9bdu2BvGj_Zc2txvUej

# **Практическое задание 2**
Задачи:
Для 1 и 2-й задачи используйте датасет insurance
1. Построить график, отображающий зависимость стоимости страховки от признака на выбор (1 балла)
2. Построить и обучить регрессию с помощью градиентного спуска зависимости от индекса массы тела (bmi) **курящих людей** и стоимости страховки, протестировать ее, сделать вывод. Оценивание:
  1. Выделение индекса массы тела только курящих людей из датасета (1 балл)
  2. Настроены гиперпараметры и градиентный спуск (1 балл)
  3. Процесс обучения идет успешно, т.е. функция ошибки со временем уменьшается (1 балл)
3. Построить и обучить нейросеть для задачи линейной регресии зашумленной зависимости с одним выходом первого слоя (2 балла)
4. Построить и обучить нейросеть для задачи линейной регресии с данными из задачи 3 с **более чем** одним выходом из первого линейного слоя (выход первого слоя строго больше 1) (2.5 балла)
5. (Бонус) Построить и обучить нейросеть для задачи линейной регресии зашумленной зависимости с **тремя слоями** и ошибкой **не более 0.25** (1.5 балла)

В этой практике работа будет с датасетом insurance, в котором хранятся разные данные о людях с разной стоимостью медицинских услуг. Общий "скелет" моделей готов, его необходимо доработать и сделать выводы о каждом этапе задания.
Готовый код в ячейках **необязательно использовать***, можно заменить его на любой подходящий, главное - решение задач.

*Исключение: менять датасет для регрессии или зашумленную функцию в 3-5 заданиях на более простую - читинг 😬

*Перед работой над практикой, не забудьте загрузить в виртуальную среду архив с файлом датасета* **med.zip**.

## 1. Построить график, отображающий зависимость стоимости страховки от признака на выбор (1 балл)
График должен отображать некую зависимость, благодаря которой можно сделать какой-то вывод.
"""

import torch
import torchvision
import torch.nn as nn
import pandas as pd
import seaborn as sns
import matplotlib.pyplot as plt
import torch.nn.functional as F
from torchvision.datasets.utils import download_url
from torch.utils.data import DataLoader, TensorDataset, random_split
import numpy as np

!unzip 'med.zip'
!ls

data = pd.read_csv("insurance.csv")
data.head()

data.info()

data['bmi']

"""Добавьте необходимые функции"""

plt.figure(figsize=(10, 7))
plt.scatter(data['bmi'], data['charges'])
plt.xlabel('Индекс массы тела') # Подпишите колонки
plt.ylabel('Стоимость страховки') # Подпишите колонки
plt.title('Зависимость стоимости страховки от индекса массы тела'); # Подпишите зависимость

"""## 2. Построить и обучить регрессию с помощью градиентного спуска зависимости стоимости медицинской страховки от индекса массы тела (bmi) **курящих людей**, протестировать ее, сделать вывод

Модифицируйте датасет таким образом, чтобы переменная attr содержала в себе только данные индекса массы тела у **курящих людей**
"""

pr = data[data['smoker'] == 'yes'] # Здесь нужно модифицировать так, чтобы в колонке smokers было только значение yes
print(pr.head())

attr = pr['bmi'] / max(pr['bmi'])
target = pr['charges'] / 100

"""Сейчас датасет содержит в себе информацию в том числе о некурящих людях"""

x = torch.FloatTensor(attr) # преобразуйте attr к Tensor, состоящему из данных типа float
y = torch.FloatTensor(target) # преобразуйте target к Tensor, состоящему из данных типа float

"""**Напишите функцию MSE**:"""

def mse_m(y_pred,y_true):
    return torch.mean((y_pred - y_true)**2) # Заменить на вывод MSE

w = torch.zeros(1, requires_grad = True)
b = torch.zeros(1, requires_grad = True)

y_pred = w * x + b
loss = mse_m(y_pred,y)
loss.backward()

loss

y_pred = w * x + b
loss = mse_m(y_pred,y)
loss.backward()

print('dl/dw = ', w.grad)
print('dl/db = ', b.grad)

w.grad.zero_()
b.grad.zero_()
w.grad, b.grad

from IPython.display import clear_output

def show_progress(x,y,y_pred,loss):
    y_pred = y_pred.detach()
    loss = loss.item()
    clear_output(wait=True)
    plt.figure(figsize=(10,7))
    plt.scatter(x,y,alpha = 0.75)
    plt.scatter(x,y_pred,color = 'orange',linewidth = 5)
    plt.show()

    print(f"MSE = {loss:.3f}")

loss = 0

"""Замените нужные гиперпараметры на корректные значения."""

w = torch.zeros(1, requires_grad = True) #параметр requires_grad = True нужен для возможности вычисления градиента
b = torch.zeros(1, requires_grad = True)
num_iter = 1000

lr_w = 0.1
lr_b = 0.1

for i in range(num_iter):
    y_pred = w*x + b
    loss = mse_m(y_pred,y) # подсчитайте ошибку MSE
    loss.backward() # сделайте спуск по ошибке (backward)

    w.data -= lr_w * w.grad.data # Допишите то, чего не хватает для обучения параметра w
    b.data -= lr_b * b.grad.data # Допишите то, чего не хватает для обучения параметра b

    w.grad.zero_()
    b.grad.zero_()
    if (i+1)%5 ==0:
        show_progress(x,y,y_pred,loss)
        if loss.item()<5000: # Настройте оптимальную остановку обучения
            print('Done')
            print(i)
            break

"""Сделайте вывод о найденой зависимости.

Вывод: стоимость медицинской страховки прямо пропорционально зависит от bmi курящих людей.

## Построить и обучить нейросеть для задачи линейной регресии зашумленной зависимости с одним выходом первого слоя (2 балла)

Если у вас не работает обучение, попробуйте перезагрузить ядро
"""

np.random.seed(0)
x=2*torch.rand(100)
y=4+3*x**2 + torch.rand(100) # Генерируем случайную зашумленную зависимость

x = torch.FloatTensor(x)
y = torch.FloatTensor(y)

x_new = x.reshape(-1,1)
y_new = y.reshape(-1,1)

model1 = nn.Sequential(
    nn.Linear(in_features=1,out_features=1),
    nn.Tanh(),
    nn.Linear(in_features=1,out_features=1)
)
model1

optimizer = torch.optim.SGD(model1.parameters(), lr = 0.01) # Настройте гиперпараметры, lr - learning rate.

optim_func = nn.MSELoss() #Н Настройте метрику - MSE. Подсказка: метрика есть в методах класса nn

num_iter = 1000
loss =0

for i in range(num_iter):
    y_pred = model1(x_new)
    loss = optim_func(y_pred,y_new)
    loss.backward() # 1. уменьшение функции ошибки
    optimizer.step() # 2. шаг градиента
    optimizer.zero_grad() # 3. обнуление градиента?

    if (i+1)%5 ==0:
        show_progress(x,y,y_pred,loss)

        if loss.item()<1: # Настройте оптимальную остановку обучения
            print('Done')
            print(i)
            break

"""Обучите модель. Если модель плохо обучается, присмотритесь к optimizer.

## 4. Построить нейросеть для задачи линейной регресии зашумленной зависимости с **более чем** одним выходом на первом слое  (2.5 балла)
"""

np.random.seed(0)
x=2*torch.rand(100)
y=4+3*x**2 + torch.rand(100) # Генерируем случайную зашумленную зависимость

x = torch.FloatTensor(x)
y = torch.FloatTensor(y)

x_new = x.reshape(-1,1)
y_new = y.reshape(-1,1)

model2 = nn.Sequential(
    nn.Linear(in_features=1,out_features=10),
    nn.Tanh(),
    nn.Linear(in_features=10,out_features=1)
)
model2

optimizer = torch.optim.SGD(model2.parameters(), lr = 0.01) # скопируйте код обучения из предыдущей задачи (заменив на model2), протестируйте нейросеть, сделайте вывод

num_iter = 1000
loss =0

for i in range(num_iter):
    y_pred = model2(x_new)
    loss = optim_func(y_pred,y_new)
    loss.backward() # 1. уменьшение функции ошибки
    optimizer.step() # 2. шаг градиента
    optimizer.zero_grad() # 3. обнуление градиента?

    if (i+1)%5 ==0:
        show_progress(x,y,y_pred,loss)

        if loss.item()<0.25: # Настройте оптимальную остановку обучения
            print('Done')
            print(i)
            break

"""Вывод: нейросеть с данной моделью сработала эффективнее прошлой, примерно за одинаковое количество иттераций смогла прийти к более малому значению ошибки.

## 5. (Бонус) Построить и обучить нейросеть для задачи линейной регресии зашумленной зависимости с **тремя слоями** и двумя слоями активации и ошибкой **не более 0.25** (1.5 балла)
"""

# Подсказка: все остается прежним, меняется только архитектура модели
model3 = nn.Sequential(
    nn.Linear(in_features=1, out_features=10),
    nn.Tanh(),
    nn.Linear(in_features=10, out_features=10),
    nn.Tanh(),
    nn.Linear(in_features=10, out_features=1)
  # укажите оптимальное количество нейронов
  # настройте функцию активации (присмотритесь к этому полю, какая функция активации лучшая: сигмоида, tanh или relu)
  # Допишите оставшуюся часть (два слоя и ф-ия активации)
)
model3

"""Обучите нейросеть, сделайте вывод о ее эффективности и обоснуйте его."""

optimizer = torch.optim.SGD(model3.parameters(), lr = 0.01)
num_iter = 1000
loss =0

for i in range(num_iter):
    y_pred = model3(x_new)
    loss = optim_func(y_pred,y_new)
    loss.backward() # 1. уменьшение функции ошибки
    optimizer.step() # 2. шаг градиента
    optimizer.zero_grad() # 3. обнуление градиента?

    if (i+1)%5 ==0:
        show_progress(x,y,y_pred,loss)

        if loss.item()<0.25: # Настройте оптимальную остановку обучения
            print('Done')
            print(i)
            break

"""Вывод: нейросеть достаточно эффективна: быстро смогла решить задачу, всего за 229 иттераций(в предыдущих пунктах потребовалось больше) была достигнута ошибка меньше 0.25, модель достаточно хорошо приближает исходные данные."""